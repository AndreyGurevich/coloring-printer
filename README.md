# 🎨 Coloring Printer - Генератор раскрасок

Веб-приложение для генерации раскрасок через DALL-E 3 с возможностью печати на принтере.

## ✨ Возможности

- 🖼️ Генерация раскрасок по текстовому описанию
- 🎨 Выбор стиля: простой, мультяшный, реалистичный
- 📊 Настройка уровня детализации: низкий, средний, высокий
- 🖨️ Автоматическая печать на подключенном принтере
- 📚 Галерея всех созданных раскрасок
- 📱 Адаптивный веб-интерфейс

## 📋 Требования

- Python 3.8+
- OpenAI API ключ
- CUPS (для печати на Linux/Raspberry Pi)

## 🚀 Установка

### 1. Клонирование и установка зависимостей

```bash
cd /path/to/coloring-printer
pip install -r requirements.txt
```

### 2. Настройка

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
nano .env
```

Обязательные параметры:
```
OPENAI_API_KEY=sk-your-api-key-here
```

Опциональные параметры:
```
OUTPUT_DIR=output
ENABLE_PRINTING=false    # Установите true для автоматической печати
```

### 3. Запуск

```bash
python app.py
```

Сервер запустится на `http://0.0.0.0:5000`

## 🌐 Использование

### Веб-интерфейс

1. Откройте браузер и перейдите на `http://raspberry-pi-ip:5000`
2. Введите описание того, что хотите раскрасить
3. Выберите стиль и уровень детализации
4. Нажмите "Сделай раскраску!"
5. Скачайте или отправьте на печать

### Галерея

Все сгенерированные раскраски сохраняются в папке `output/` и доступны в галерее по адресу `http://raspberry-pi-ip:5000/gallery`

## 📁 Структура проекта

```
coloring-printer/
├── app.py                    # Flask веб-сервер
├── config.py                 # Конфигурация
├── mqtt_subscriber.py        # MQTT подписчик (опционально)
├── templates/
│   ├── index.html           # Главная страница
│   └── gallery.html         # Галерея
├── output/                  # Сгенерированные изображения
├── requirements.txt         # Зависимости
└── .env                     # Настройки (создать из .env.example)
```

## 🖨️ Настройка печати

### Быстрая настройка

1. **Просмотр доступных принтеров:**
```bash
python list_printers.py
```

2. **Настройка .env:**
```bash
ENABLE_PRINTING=true
PRINTER_NAME=              # Пустое = принтер по умолчанию
# или
PRINTER_NAME=HP_LaserJet   # Конкретный принтер
```

3. **Тестовая печать:**
```bash
python list_printers.py --test
```

4. **Перезапустите сервер**

📖 **Подробная инструкция:** См. [PRINTING_SETUP.md](PRINTING_SETUP.md)

### Установка CUPS на Raspberry Pi

```bash
sudo apt-get update
sudo apt-get install cups
sudo usermod -a -G lpadmin $USER

# Настройка принтера через веб-интерфейс
# Откройте http://localhost:631
```

## 🔧 Настройка автозапуска

### Systemd service

Создайте файл `/etc/systemd/system/coloring-printer.service`:

```ini
[Unit]
Description=Coloring Printer Web Server
After=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/coloring-printer
Environment="PATH=/home/pi/coloring-printer/venv/bin"
ExecStart=/home/pi/coloring-printer/venv/bin/python app.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активируйте сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable coloring-printer
sudo systemctl start coloring-printer
sudo systemctl status coloring-printer
```

## 🌐 Доступ из локальной сети

Найдите IP адрес Raspberry Pi:

```bash
hostname -I
```

Откройте в браузере с любого устройства в локальной сети:
```
http://192.168.x.x:5000
```

## 📝 API для интеграций

Сервер предоставляет REST API для интеграции с любыми системами:

- 🏠 **Home Assistant** - автоматизации умного дома
- 🤖 **Node-RED** - визуальное программирование
- 🎤 **Голосовые ассистенты** - свой или Алиса/Google
- 💬 **Telegram боты** - через мессенджер
- 📱 **iOS Shortcuts** - команды Siri
- ⏰ **Cron** - по расписанию
- 🔌 **IFTTT/Zapier** - триггеры и действия

📖 **Примеры интеграций:** См. [INTEGRATIONS.md](INTEGRATIONS.md)

### Основные endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Веб-интерфейс |
| `/generate` | POST | Генерация раскраски |
| `/print` | POST | Печать файла |
| `/gallery` | GET | Галерея раскрасок |
| `/output/<filename>` | GET | Скачать файл |

## 💡 Примеры использования

**Простые раскраски для малышей:**
- "кот" + простой стиль + низкая детализация

**Мультяшные персонажи:**
- "веселый дракон" + мультяшный стиль + средняя детализация

**Сложные раскраски:**
- "замок с башнями" + реалистичный стиль + высокая детализация

## 🐛 Решение проблем

### Ошибка "OPENAI_API_KEY not set"
Проверьте файл `.env` и убедитесь, что API ключ указан правильно

### Принтер не печатает
1. Проверьте, что CUPS установлен: `lpstat -p`
2. Проверьте, что принтер подключен и включен
3. Проверьте настройку `ENABLE_PRINTING=true` в `.env`

### Сервер недоступен из сети
1. Проверьте IP адрес: `hostname -I`
2. Проверьте, что порт 5000 не заблокирован файрволом
3. На Raspberry Pi может потребоваться открыть порт:
   ```bash
   sudo ufw allow 5000
   ```

## 📊 Стоимость использования

DALL-E 3 API:
- Standard quality (1024x1024): ~$0.04 за изображение
- HD quality (1024x1024): ~$0.08 за изображение

Проект использует standard quality для оптимизации затрат.

## 🔄 Обновление

```bash
git pull
pip install -r requirements.txt --upgrade
sudo systemctl restart coloring-printer
```

## 📜 Лицензия

MIT

## 🤝 Вклад

Pull requests приветствуются!

---

Сделано с ❤️ для детского творчества
