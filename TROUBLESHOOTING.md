# 🔧 Решение проблем

## 🖨️ Печать не работает из веб-интерфейса

### Проблема: "lp command not found"

**Симптом:** Из консоли печать работает, но из веб-интерфейса выдает ошибку `lp command not found`.

**Причина:** Flask/systemd не видит команду `lp` в PATH.

### ✅ Решение:

#### Шаг 1: Найдите полный путь к команде lp

```bash
which lp
```

Обычно выводит: `/usr/bin/lp`

#### Шаг 2: Добавьте в .env

Откройте файл `.env`:
```bash
nano ~/.env
```

Добавьте или измените строку:
```bash
LP_COMMAND_PATH=/usr/bin/lp
```

#### Шаг 3: Перезапустите сервер

**Если запущен вручную:**
```bash
# Остановите (Ctrl+C)
# Запустите заново
python3 app.py
```

**Если запущен как systemd сервис:**
```bash
sudo systemctl restart coloring-printer
```

#### Шаг 4: Проверьте логи

```bash
# Если запущен вручную - смотрите в консоли

# Если systemd сервис:
sudo journalctl -u coloring-printer -f
```

При попытке печати должны увидеть:
```
INFO:__main__:Running command: /usr/bin/lp /home/pi/coloring-printer/output/coloring_test_20240115_123456.png
INFO:__main__:Printed: /home/pi/coloring-printer/output/coloring_test_20240115_123456.png
```

---

## 🔍 Альтернативное решение: Настройка PATH в systemd

Если не хотите менять `.env`, можно настроить PATH в systemd сервисе:

```bash
sudo nano /etc/systemd/system/coloring-printer.service
```

Добавьте полный PATH:
```ini
[Service]
Environment="PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
```

Перезагрузите systemd:
```bash
sudo systemctl daemon-reload
sudo systemctl restart coloring-printer
```

---

## 🐛 Другие частые проблемы

### Принтер печатает, но неправильно

**Проблема:** Изображение обрезано или искажено.

**Решение:** Добавьте опции печати в `.env`:
```bash
LP_OPTIONS=-o fit-to-page -o media=A4
```

Модифицируйте `app.py`:
```python
cmd = [lp_path, '-o', 'fit-to-page', '-o', 'media=A4', filepath]
```

### CUPS не запущен

**Проверка:**
```bash
sudo systemctl status cups
```

**Запуск:**
```bash
sudo systemctl start cups
sudo systemctl enable cups
```

### Принтер не в сети / выключен

**Проверка:**
```bash
lpstat -p
```

Должно быть: `printer is idle`

Если: `printer is not ready` - проверьте подключение и питание принтера.

### Нет прав на печать

**Проверка:**
```bash
groups
```

Должна быть группа `lpadmin`.

**Исправление:**
```bash
sudo usermod -a -G lpadmin pi
# Выйдите и войдите заново, или:
sudo reboot
```

### OpenAI API ошибка

**Проблема:** `OPENAI_API_KEY not set`

**Решение:**
```bash
nano ~/.coloring-printer/.env
```

Проверьте что ключ указан:
```bash
OPENAI_API_KEY=sk-ваш-ключ-здесь
```

### Сервер недоступен из сети

**Проверка файрвола:**
```bash
sudo ufw status
```

**Открыть порт:**
```bash
sudo ufw allow 5000
```

### Долгая генерация / таймауты

**Проблема:** Генерация занимает >30 секунд и происходит таймаут.

**Решение:** Увеличьте таймаут в Gunicorn (если используете):
```bash
gunicorn --timeout 120 app:app
```

Или в systemd:
```ini
[Service]
TimeoutStartSec=120
```

---

## 📊 Диагностика

### Проверить что все работает:

```bash
# 1. CUPS запущен
sudo systemctl status cups

# 2. Принтер подключен
lpstat -p -d

# 3. Тестовая печать
python3 list_printers.py --test

# 4. Flask сервер запущен
sudo systemctl status coloring-printer

# 5. Порт открыт
sudo netstat -tlnp | grep 5000

# 6. Логи
sudo journalctl -u coloring-printer -n 50
```

### Тестовая печать через curl:

```bash
curl -X POST http://localhost:5000/generate \
  -H "Content-Type: application/json" \
  -d '{"subject":"test","style":"simple","detail_level":"low","print":true}'
```

---

## 🆘 Если ничего не помогло

1. **Соберите диагностику:**
```bash
which lp
echo $PATH
lpstat -p -d
sudo systemctl status cups
sudo systemctl status coloring-printer
sudo journalctl -u coloring-printer -n 100
```

2. **Проверьте права на файлы:**
```bash
ls -la ~/coloring-printer/
ls -la ~/coloring-printer/output/
```

3. **Попробуйте запустить вручную:**
```bash
cd ~/coloring-printer
source venv/bin/activate
python3 app.py
# Попробуйте напечатать через веб-интерфейс
# Смотрите ошибки в консоли
```

4. **Тестовая печать напрямую:**
```bash
/usr/bin/lp ~/coloring-printer/output/любой_файл.png
```

Если последняя команда работает, но из Flask нет - проблема в правах или окружении.
